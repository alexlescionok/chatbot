[project]
name = "chatbot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.138.0",
    "psycopg[binary]>=3.3.4",
    "pydantic-ai>=1.107.0",
    "pydantic-ai-slim[web-fetch]>=1.107.0",
    "python-dotenv>=1.2.2",
]
[tool.fastapi]
entrypoint = "greet:app"
dependencies = [
    "fastapi[standard]>=0.135.3",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.0.3",
]
